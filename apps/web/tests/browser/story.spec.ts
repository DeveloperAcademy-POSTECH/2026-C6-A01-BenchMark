import { test, expect } from "@playwright/test";
import sharp from "sharp";
import { mkdir } from "node:fs/promises";
import { Pool } from "pg";

test("admin registration, publication, mobile reading and one-device reaction", async ({ page, browser }) => {
  if (!process.env.DATABASE_URL?.endsWith("/benchmark_mat_test")) throw new Error("Use the isolated test database only.");
  const pool = new Pool({ connectionString: process.env.DATABASE_URL });
  const photo = await sharp({ create: { width: 1000, height: 750, channels: 3, background: "#e4dec4" } }).composite([{ input: Buffer.from('<svg width="1000" height="750"><text x="500" y="390" text-anchor="middle" font-family="sans-serif" font-size="42" fill="#536342">TEST PHOTO</text></svg>') }]).jpeg().toBuffer();
  const errors: string[] = []; page.on("pageerror", (error) => errors.push(error.message));
  let storyId = ""; let reservationId = "";
  try {
    await page.goto("/admin");
    await page.getByLabel("비밀번호", { exact: true }).fill(process.env.ADMIN_PASSWORD!);
    await page.getByRole("button", { name: "관리자 페이지로 이동" }).click();
    await expect(page.getByRole("heading", { name: "이야기 관리" })).toBeVisible();
    await page.getByRole("button", { name: "+ 새 이야기 등록" }).click();
    await page.getByLabel("돗자리 번호", { exact: true }).fill("9902");
    await page.getByLabel("공개 이름").fill("검증용 운영팀");
    await page.getByLabel("제목", { exact: true }).fill("함께 쉬어 가는 자리에 남긴 마음");
    await page.getByLabel("사진 한 장").setInputFiles({ name: "test.jpg", mimeType: "image/jpeg", buffer: photo });
    await page.getByRole("textbox", { name: /^이야기/ }).fill("이 화면은 검증용 데이터입니다. 함께 쉬어 가는 자리에 작은 마음을 남깁니다.");
    await page.getByRole("textbox", { name: /^이야기/ }).fill("🧺".repeat(250) + "a");
    await expect(page.getByRole("button", { name: "비공개로 저장" })).toBeDisabled();
    await page.getByRole("textbox", { name: /^이야기/ }).fill("🧺".repeat(250));
    await expect(page.locator("#story-length")).toHaveText("1000 / 1000바이트");
    await expect(page.getByRole("button", { name: "비공개로 저장" })).toBeEnabled();
    await expect(page.getByLabel("내용 검토 완료 · 웹에 공개")).toBeDisabled();
    await page.getByLabel("입금 확인 완료").check();
    await page.getByLabel("내용 검토 완료 · 웹에 공개").check();
    await page.getByRole("button", { name: "저장하고 공개" }).click();
    await expect(page.getByRole("heading", { name: "#9902 · 검증용 운영팀" })).toBeVisible();
    storyId = (await pool.query("SELECT id FROM mat_stories WHERE mat_number=9902")).rows[0].id;
    const screenshotDir = test.info().outputPath("screenshots"); await mkdir(screenshotDir, { recursive: true });
    await page.setViewportSize({ width: 1440, height: 1000 });
    await page.screenshot({ path: `${screenshotDir}/admin-desktop.png`, fullPage: true });
    await page.goto("/");
    await page.evaluate(() => document.fonts.ready);
    await expect(page.locator(".story-reading h1")).toHaveText("함께 쉬어 가는 자리에 남긴 마음");
    await expect(page.getByRole("heading", { name: "사용 안내서" })).toBeVisible();
    await expect(page.locator(".reaction-button")).toHaveCount(4);
    await page.screenshot({ path: `${screenshotDir}/home-desktop.png`, fullPage: true });
    await page.setViewportSize({ width: 390, height: 844 });
    await page.screenshot({ path: `${screenshotDir}/home-mobile.png`, fullPage: true });
    await page.goto("/stories");
    await expect(page.locator(".story-card")).toHaveCount(1);
    await page.setViewportSize({ width: 1440, height: 1000 });
    await page.screenshot({ path: `${screenshotDir}/stories-desktop.png`, fullPage: true });
    await page.setViewportSize({ width: 390, height: 844 });
    await page.screenshot({ path: `${screenshotDir}/stories-mobile.png`, fullPage: true });
    await page.goto(`/stories/${storyId}`);
    const reaction = page.getByRole("button", { name: /공감해요/ });
    await expect(reaction).toBeEnabled(); await reaction.click();
    await expect(page.getByRole("button", { name: /공감해요/ })).toHaveAttribute("aria-pressed", "true");
    await page.getByRole("button", { name: /응원해요/ }).click();
    await expect(page.getByRole("button", { name: /응원해요/ })).toHaveAttribute("aria-pressed", "true");
    await page.reload(); await expect(page.getByRole("button", { name: /공감해요/ })).toHaveAttribute("aria-pressed", "true");
    await page.screenshot({ path: `${screenshotDir}/story-mobile.png`, fullPage: true });
    for (const width of [320, 390, 768, 1440]) {
      await page.setViewportSize({ width, height: 900 });
      expect(await page.evaluate(() => document.documentElement.scrollWidth <= innerWidth)).toBe(true);
    }
    const other = await browser.newContext();
    try {
      const otherPage = await other.newPage(); await otherPage.goto(`/stories/${storyId}`);
      await expect(otherPage.getByRole("button", { name: /공감해요/ })).toBeEnabled();
    } finally { await other.close(); }
    await page.setViewportSize({ width: 390, height: 844 }); await page.goto("/about");
    await expect(page).toHaveURL(`/stories/${storyId}`);
    await expect(page.getByRole("link", { name: /나도 이야기 남기기/ })).toHaveAttribute("href", `/reserve?from=${storyId}`);
    await expect(page.locator('a[href="/about"]')).toHaveCount(0);
    await expect(page.locator('a[href*="naver.me"]')).toHaveCount(0);
    await page.goto(`/reserve?from=${storyId}`);
    await page.screenshot({ path: `${screenshotDir}/reservation-mobile.png`, fullPage: true });
    const dialog = page.getByRole("dialog", { name: "왜 기부 예약을 하시게 되었나요?" });
    await expect(dialog).not.toBeVisible();
    await expect(page.getByText("왜 기부 예약을 하시게 되었나요?", { exact: true })).not.toBeVisible();
    const legends = await page.locator(".reservation-fields > .reservation-question > legend").allTextContents();
    expect(legends.map(t => t.slice(0,2))).toEqual(["01","02","03","04"]);
    expect(legends[0]).toContain("이야기"); expect(legends[1]).toContain("성함");
    await page.getByRole("textbox", { name: "성함", exact: true }).fill("검증용 웹 예약자");
    await expect(page.locator('input[name="phone"]')).toHaveCount(0);
    await page.getByRole("textbox", { name: "이메일", exact: true }).fill("named@example.test");
    await page.getByRole("textbox", { name: "마음을 담은 한 줄", exact: true }).fill("다음 사람에게도 편안한 쉼을");
    await page.getByRole("textbox", { name: "스토리", exact: true }).fill("가".repeat(333) + "ab");
    await expect(page.getByRole("button", { name: "기부 예약 접수하기" })).toBeDisabled();
    await page.getByRole("textbox", { name: "스토리", exact: true }).fill("가".repeat(333) + "a");
    await expect(page.locator("#reservation-bytes")).toHaveText("1000 / 1000바이트");
    await expect(page.getByRole("button", { name: "기부 예약 접수하기" })).toBeEnabled();
    await page.locator('input[name="photo"]').setInputFiles({ name: "test.jpg", mimeType: "image/jpeg", buffer: photo });
    await page.getByLabel("간편결제", { exact: true }).check();
    const amount = page.getByRole("spinbutton", { name: "기부금액", exact: true });
    await expect(amount).toHaveAttribute("min", "10000");
    await expect(amount).toHaveAttribute("step", "1");
    await expect(page.locator(".base-amounts")).toContainText("10,000원");
    await expect(page.locator(".base-amounts")).toContainText("20,000원");
    await amount.fill("9999");
    await page.getByRole("button", { name: "기부 예약 접수하기", exact: true }).click();
    await expect(dialog).not.toBeVisible();
    expect(await amount.evaluate(input => (input as HTMLInputElement).validity.rangeUnderflow)).toBe(true);
    await amount.fill("10000");
    for (const width of [320,390,768,1440]) {
      await page.setViewportSize({width,height:900});
      expect(await page.evaluate(() => document.documentElement.scrollWidth <= innerWidth)).toBe(true);
    }
    await page.setViewportSize({width:390,height:844});
    await page.screenshot({path:`${screenshotDir}/reservation-filled-mobile.png`,fullPage:true});
    const requests: string[] = [];
    let failNext = true;
    await page.route("**/api/reservations", async route => {
      requests.push(route.request().postDataBuffer()!.toString());
      if (failNext) { failNext = false; await route.fulfill({status:503,contentType:"application/json",body:JSON.stringify({error:"검증용 접수 오류"})}); }
      else await route.continue();
    });
    const open = page.getByRole("button", { name: "기부 예약 접수하기", exact: true });
    await open.focus(); await open.press("Enter");
    await expect(dialog).toBeVisible(); expect(requests).toHaveLength(0);
    await dialog.getByRole("button", { name: "예약 접수 완료하기" }).click();
    expect(requests).toHaveLength(0);
    await dialog.getByLabel("기타", { exact: true }).check();
    await dialog.getByRole("button", { name: "예약 접수 완료하기" }).click();
    expect(requests).toHaveLength(0);
    await dialog.getByRole("textbox", { name: "기타 이유" }).fill("팝업에서 작성한 검증 이유");
    await page.keyboard.press("Escape");
    await expect(dialog).not.toBeVisible(); await expect(open).toBeFocused();
    await expect(page.getByRole("textbox", { name:"성함",exact:true })).toHaveValue("검증용 웹 예약자");
    await expect(page.getByAltText("선택한 사진 미리보기")).toBeVisible();
    await open.click(); await expect(dialog.getByRole("textbox", { name: "기타 이유" })).toHaveValue("팝업에서 작성한 검증 이유");
    await dialog.getByRole("button", { name: "예약 이유 질문 닫기" }).click();
    await expect(dialog).not.toBeVisible(); await open.click();
    await mkdir("../../docs/screenshots", { recursive: true });
    await page.screenshot({ path: "../../docs/screenshots/reservation-reason-mobile.png", fullPage: false });
    await page.setViewportSize({ width: 1440, height: 1000 });
    await page.screenshot({ path: "../../docs/screenshots/reservation-reason-desktop.png", fullPage: false });
    await dialog.getByRole("button", { name: "예약 접수 완료하기" }).press("Enter");
    await expect(dialog.getByRole("alert")).toHaveText("검증용 접수 오류");
    expect(requests).toHaveLength(1);
    await expect(dialog.getByRole("textbox", { name: "기타 이유" })).toHaveValue("팝업에서 작성한 검증 이유");
    await dialog.getByRole("button", { name: "예약 접수 완료하기" }).press("Enter");
    await expect(page.getByRole("heading", { name:"기부 예약이 접수됐어요." })).toBeVisible();
    expect(requests).toHaveLength(2);
    const sentId = (body: string) => /name="id"\r?\n\r?\n([^\r\n]+)/.exec(body)?.[1];
    expect(sentId(requests[0])).toBeTruthy(); expect(sentId(requests[0])).toBe(sentId(requests[1]));
    reservationId = sentId(requests[1])!;
    expect((await pool.query("SELECT phone FROM mat_reservations WHERE id=$1", [reservationId])).rows[0].phone).toBeNull();
    await page.goto("/admin");
    await page.getByText("검증용 웹 예약자 · 10,000원 예약", {exact:true}).click();
    await expect(page.locator('a[href^="tel:"]')).toHaveCount(0);
    await page.getByRole("button", {name:"이 예약으로 이야기 등록"}).click();
    await expect(page.getByLabel("공개 이름")).toHaveValue("검증용 웹 예약자");
    await expect(page.locator('input[name="photo"]')).not.toHaveAttribute("required");
    await page.getByRole("button", {name:"닫기",exact:true}).click();
    await page.getByRole("button", { name: "수정·검토" }).click();
    await page.getByLabel("내용 검토 완료 · 웹에 공개").uncheck();
    await page.getByRole("button", { name: "비공개로 저장" }).click();
    await expect(page.getByText("비공개", { exact: true })).toBeVisible();
    await page.getByRole("button", { name: "로그아웃" }).click();
    await expect(page.getByRole("button", { name: "관리자 페이지로 이동" })).toBeVisible();
    await page.goto(`/stories/${storyId}`); await expect(page.getByRole("heading", { name: "아직 펼쳐지지 않은 이야기예요." })).toBeVisible();
    await page.goto("/");
    await expect(page).toHaveURL("/stories");
    await expect(page.locator(".empty-state h2")).toContainText("펼칠 준비를 하고 있어요.");
    await expect(page.getByRole("link", { name: /나도 이야기 남기기/ })).toHaveAttribute("href", "/reserve");
    expect(errors).toEqual([]);
  } finally {
    if (reservationId) await pool.query("DELETE FROM mat_reservations WHERE id=$1", [reservationId]);
    if (storyId) await pool.query("DELETE FROM mat_stories WHERE id=$1", [storyId]);
    await pool.end();
  }
});
